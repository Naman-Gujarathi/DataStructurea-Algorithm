package DynamicProgramming;

public class NinjaTraining {

    maxPoints () {

        for(int day=1; day<arr.length; day++) {
            if()
            int value = arr[0] + maxPoints()
            if()
            int value1 = arr[1] + maxPoints()
            if()
            int value2 = arr[2] + maxPoints()
        }
        int max= Math.max(value, value1);
        return Math.max(value1, value2);

      
    }


    public static void main (String args[]) {
        NinjaTraining obj = new NinjaTraining();
        obj.maxPoints()
    }
    
}
